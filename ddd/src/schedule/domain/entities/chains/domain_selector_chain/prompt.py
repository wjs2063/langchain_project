from langchain_core.prompts import PromptTemplate

selector_prompt = PromptTemplate.from_template(
    template="""너는 AI 도메인 분류 시스템입니다.  
사용자의 **대화 기록과 현재 질문을 종합적으로 분석**하여, 적절한 도메인을 분류하세요.  

# 분류 대상 (반드시 3가지 중 하나 선택)
- **weather**: 날씨, 기온, 강수량, 기후, 미세먼지 등과 관련된 질문  
- **schedule**: 일정, 시간표, 약속, 미팅, 예약, 이벤트 등과 관련된 질문  
- **general**: 위 두 범주에 해당하지 않는 일반적인 질문  

---

## 📌 **분류 기준**
1. **현재 질문에 날씨 관련 대화흐름이 포착되거나  
   최근 대화에서 날씨에 대한 질문이 있었다면 → `weather` 선택  
2. **현재 질문에 일정 관련 대화흐름이 포착되거나  
   최근 대화에서 일정에 대한 질문이 있었다면 → `schedule` 선택  
3. 위 두 가지 범주에 해당하지 않는다면 → **무조건 `general`을 선택**  

---

## 📌 **출력 형식 (반드시 하나만 출력)**
반드시 아래 3가지 중 **하나만 출력**하세요:  
- `weather`
- `schedule`
- `general`  
**추가적인 단어나 설명을 포함하지 마세요.**

---

## 입력 데이터:
대화 기록:
{chat_history}

현재 질문:
{question}

카테고리:
"""
)
